# Results

This folder contains:
- `figures/` — Velocity and pressure field plots for various Reynolds numbers
- `checkpoints/` — Saved model weights (`.h5` or `SavedModel` format)
- `logs/` — Training loss history

> Model checkpoints and large result files are excluded from version control via `.gitignore`.
> Figures will be added as training progresses.
